table(cellpadding='0', cellspacing='0', height='auto', width='240')
  tbody
    tr
      td.title(valign='middle', align='center')
        h3.center
          em In This Issue:
    - var countStories = 1;
    - each story in data.stories
      tr
        td(valign='top', align='left')
          p
            a.no-underline(href='#toc_item_#{ countStories }') !{ story.title }
      - countStories = countStories + 1;
